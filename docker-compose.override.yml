# Development-specific docker-compose settings
# This file is automatically merged with docker-compose.yml
version: '3.8'

services:
  backend:
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
